# AWS Configuration
# The backend is fully serverless - no local server needed!
# After deploying your CDK stack, copy this file to .env and update the API URL

# API Gateway endpoint URL (get this from CDK deploy output)
# Example: https://abc123def.execute-api.us-east-1.amazonaws.com/prod
VITE_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod